/*
练习：编写一个函数，可以从 BCS 字节中提取值以重建给定的结构体：

struct Info {
    a: bool,
    b: u8,
    c: u64,
    d: u128,
    k: vector<bool>,
    s: address
}

该函数应接受一个字节向量并返回该结构体。
*/

public fun deserialize(bytes: vector<u8>): Info {
    // 这里填写代码
}